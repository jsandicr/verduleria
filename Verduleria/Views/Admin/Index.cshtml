@model IEnumerable<Verduleria.Models.Producto>
@{
    ViewData["Title"] = "Verduleria";
    Layout = "_AdminLayout";
}
<!-- Navbar End -->
@if (Model != null)
{
    <div class="container-fluid pt-4 px-4">
        <div class="row g-4">
            <div class="col-12">
                <div class="bg-light rounded h-100 p-4">
                    <h6 class="mb-4">Productos</h6>
                    <a class="btn btn-primary" asp-action="AgregarProducto" asp-controller="Admin">Crear nuevo</a>
                    <div class="table-responsive">
                        <table class="table">
                            <thead>
                                <tr>
                                    <th scope="col">
                                        @Html.DisplayNameFor(model => model.Nombre)
                                    </th>
                                    <th scope="col">
                                        @Html.DisplayNameFor(model => model.Descripcion)
                                    </th>
                                    <th scope="col">
                                        @Html.DisplayNameFor(model => model.Cantidad)
                                    </th>
                                    <th scope="col">
                                        @Html.DisplayNameFor(model => model.Activo)
                                    </th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach(var producto in Model){
                                    <tr>
                                        <td>
                                            @Html.DisplayFor(modelItem => producto.Nombre)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => producto.Descripcion)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => producto.Cantidad)
                                        </td>
                                        <td>
                                            @Html.DisplayFor(modelItem => producto.Activo)
                                        </td>
                                        <td>
                                            <a asp-action="EditarProducto" asp-route-id="@producto.Id">Editar</a>
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>
}
<!-- Content End -->


<!-- Back to Top -->
<a href="#" class="btn btn-lg btn-primary btn-lg-square back-to-top"><i class="bi bi-arrow-up"></i></a>